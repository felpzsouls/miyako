doctype html
html(lang="pt-br")
    head
        include includes/js.pug
        include includes/head.pug
        
        title !{bot.user.username} || Dashboard
    body 
        include includes/navbar.pug
        
        .container-md.mt-5.p-3.rounded(style="background-color: #FA5532")
            .row
                .col-12.col-md-4.border-end.border-dark
                    .container-md.d-flex.flex-column.align-items-center
                        img.img.thumbnail.rounded-circle.border.border-info(src=avatar)
                        h2.text-center 
                            | !{user.username}
                    .nav.flex-column
                        a.nav-link.active(href="/dashboard") Servidores